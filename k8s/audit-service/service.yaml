apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: university-platform
  labels:
    app: audit-service
    part-of: university-platform
spec:
  type: ClusterIP
  selector:
    app: audit-service
  ports:
    - port: 8084
      targetPort: 8084
