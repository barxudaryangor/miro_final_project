apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-academic-pvc
  namespace: university-platform
  labels:
    app: postgres-academic
    part-of: university-platform
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-academic
  namespace: university-platform
  labels:
    app: postgres-academic
    part-of: university-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-academic
  template:
    metadata:
      labels:
        app: postgres-academic
        part-of: university-platform
    spec:
      containers:
        - name: postgres-academic
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: "academic_db"
            - name: POSTGRES_USER
              value: "academic_user"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: university-secrets
                  key: academic-db-password
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: postgres-academic-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-academic
  namespace: university-platform
  labels:
    app: postgres-academic
    part-of: university-platform
spec:
  type: ClusterIP
  selector:
    app: postgres-academic
  ports:
    - port: 5432
      targetPort: 5432
