apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: university-platform
  labels:
    app: auth-service
    part-of: university-platform
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - port: 8081
      targetPort: 8081
